{
  "cases": [
    {
      "mode": "fp32",
      "exact_tensor_comparisons": 452,
      "operations": 10
    }
  ],
  "device": "cpu",
  "threads": 1,
  "scope": "Native global memory write -> object insertion/history rebuild -> forward/reverse propagation -> correction/preflight, resident/offloaded/paged equivalence, plus actual source encoder comparisons immediately after global rewrite and after bucket rebuild, using channels-last shared image features. Predicted low masks/logits unchanged by memory rewrite; obsolete overrides cleared on preflight."
}
