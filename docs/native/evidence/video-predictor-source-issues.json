{
  "model": "sam3.1",
  "unmodified_source_result": "IndexError: list index out of range in _batch_find_inputs at chunk_find_inputs[0]",
  "trigger": "add_prompt(frame=0,text=person), propagate(start=0,max_frame_num_to_track=3), grounding batch1",
  "source_generator": "includes start+max_frame_num_to_track",
  "source_detector": "exclusive chunk_end=start+max_frame_num_to_track",
  "reference_adapter": "Opt-in --sam31-inclusive-grounding-bound increments only the forward detector bound by one; generator/output scheduling unchanged.",
  "scope": "Actual original invocation failed. Adapted report retains non-exact semantic lifecycle results.",
  "precision_audit": {
    "source_flags_inferred_from_constructor": {
      "matmul_tf32": true,
      "cudnn_tf32": true
    },
    "native_tf32": false,
    "flags_recorded_during_original_run": false,
    "evidence": "Sam3MultiplexVideoPredictor.__init__ unconditionally enables both flags after this driver set them false; decoder trace first diverges at its FP32 FFN. A new run enforces/records flags after construction and removes all ten mask-only mismatches.",
    "superseded_by": "video-predictor-sam31-visible.json"
  },
  "suppression_audit": {
    "source_gpu_candidate": "to_suppress_mask is computed but has no downstream read in run_tracker_update_planning_phase",
    "source_display_policy": "Uses rank0_metadata suppressed_obj_ids, not the GPU candidate",
    "fix": "Native owning predictor and coherent probe now use the published host set. A zero native keep-alive counter still displays the tracked ID, matching original output."
  }
}
